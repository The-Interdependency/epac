# Next tests and stop rules

Current decision: **SURVIVED** for finite observer-count minimum and collision
closure on two declared domains; **UNRESOLVED** for canonicality, general state
sufficiency, structural compositionality and physical correspondence.

## 1. Challenge the frozen-state minimum on new constructions

Freeze all nine minimum pairs before evaluation. Enumerate supported released
subatomic and element constructions outside the nine original symbols; list
their exact inputs and count before constructing them. Keep the nine declared
molecules as the composition baseline; admit new molecules only through actual
supported constructor inputs. No isotope, geometry or chirality inputs may be
invented and reported as released capabilities.

Compare all pairs in the expanded domain. Separate collisions in construction
identity from mismatches in a named downstream operation. A new collision
**FALSIFIES** the expanded identity claim. Record it before considering a
refinement; do not select another minimum retrospectively and call it a passed
prediction. If an expected observable is unavailable, report **UNRESOLVED** with
the exact missing contract. Passing the complete declared domain is **SURVIVED**
only for that domain.

## 2. Test sufficiency through composition

For each surviving candidate, supply equal-descriptor inputs to the same
released operation under matched parameters. Compare output descriptors and
named downstream behavior. Any mismatch **FALSIFIES** substitution safety.
The existing 126 retained-field comparisons establish extraction consistency;
they do not prove a structural aggregation law. If the population contains no
nontrivial equal-descriptor pair, the substitution test is **UNRESOLVED**, not
vacuously accepted as general compositionality.

## 3. Admit a physical target separately

Choose one measurable distinction already represented in the program's
comparison work. Specify its independent data, transformation rules, mapping,
and failure criterion before evaluation. Use held-out outcomes; record failures
without rewriting the target. Relabeling equivariance, source identity and
collision-free state labels do not establish a physical prediction.

The 14 original **FALSIFIED** standings stay intact. Chirality, cyclic equivalence
and spatial nonintersection require their own definitions and witness mappings;
an ordered tuple alone supplies none of those meanings.

## Add an observable only for an evidenced need

First determine whether the distinguishing information already exists in the
receipt and was discarded by projection. Evaluate all equal-size refinements;
record field-removal counterexamples. Count named composite observers explicitly
and do not describe that count as bits or fundamental primitives. Stop adding
fields when no target collision remains in the declared domain. Any proposed
global descriptor remains **UNRESOLVED** without a stronger argument.

## Evidence quality gate

Do not publish a SURVIVED result before its script runs successfully. Keep
exceptions, unexecuted cases, normalization failures, and observation counts in
the report. Superseded unsupported interpretations are **DEPRECATED**; actual
counterexamples remain **FALSIFIED**. Use **BLOCKED** only when a named external
requirement prevents the test, rather than for an unexamined API.
